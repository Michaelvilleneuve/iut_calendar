<div class="container">
	<h1>Choisis ta classe</h1>
	<div class="text-center">
		<% @sources.each do |source| %>
		<%= link_to source.title, source_path(source.to_param), class: 'source' %>
		<% end %>
	</div>
</div>